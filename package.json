{
  "name": "string-utils",
  "version": "1.1.0",
  "private": true,
  "description": "String utilities for internal tooling",
  "main": "string-utils.js",
  "scripts": {
    "pretest": "node scripts/telemetry.js",
    "test": "node --test"
  }
}
